{
  "name": "typedemitter-core",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git@github.com:lukasbach/typedemitter.git"
  },
  "author": "Lukas Bach",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel --npm-client=yarn",
    "build": "lerna run build --npm-client=yarn",
    "test": "lerna run test --npm-client=yarn --stream",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write ."
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.2.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
